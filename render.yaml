services:

  - type: web

    name: retirement-copilot-api

    runtime: python

    buildCommand: |
      pip install -r requirements/backend.txt

    startCommand: |
      uvicorn backend.main:app --host 0.0.0.0 --port 10000

    envVars:

      - key: OPENAI_API_KEY
        sync: false
